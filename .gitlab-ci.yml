include:
  - template: Security/Dependency-Scanning.gitlab-ci.yml
  - '/infra/gitlab/ansible-deploy.yaml'

variables:
  NEXUS_URL: "$NEXUS_URL"
  NEXUS_USER: "$NEXUS_USER"
  NEXUS_PASS: "$NEXUS_PASS"
  COMMIT_TAG: "11f4cb46"
  ANSIBLE_SSH_KEY: "$ANSIBLE_SSH_KEY"

stages:
  - test
  - build
  - infrastructure
  - acceptance

deploy to stage:
  allow_failure: false
  timeout: 15m
  image: '$NEXUS_URL/ioque/ansible'
  only:
    - master
  environment: stage
  resource_group: stage
  stage: infrastructure
  script:
    - mkdir secret
    - echo "$ANSIBLE_SSH_KEY" > secret/ansible.key
    - chmod 400 secret/ansible.key
    - export ANSIBLE_HOST_KEY_CHECKING=False
    - export ANSIBLE_CONFIG=infra/ansible/ansible.cfg
    - cd infra/ansible
    - ansible-playbook --private-key secret/ansible.key --user linux playbooks/deploy.yml -l "stage"
